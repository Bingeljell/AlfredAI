# Changelog

- **2026-03-08** > src/agent/tools/definitions/writerAgent.tool.ts, src/agent/toolPolicies.ts, tests/unit/{writerAgentTool,agentToolRegistry}.test.ts > `writer_agent` drafting tool with optional file output > Added a writing tool for structured draft generation (blog/email/memo/outline/etc.) with optional local-context files and optional save-to-path behavior; includes deterministic fallback drafting when model access fails/unavailable.
- **2026-03-08** > src/agent/tools/definitions/docQa.tool.ts, src/agent/toolPolicies.ts, tests/unit/{docQaTool,agentToolRegistry}.test.ts > `doc_qa` tool for local-document Q&A with citations > Added a docs/files Q&A tool that retrieves relevant local snippets from scoped paths, answers with explicit citations, falls back deterministically when LLM/key is unavailable, and records usage when model-backed answering is used.
- **2026-03-08** > src/agent/tools/definitions/runDiagnostics.tool.ts, src/agent/toolPolicies.ts, tests/unit/{runDiagnosticsTool,agentToolRegistry}.test.ts > `run_diagnostics` tool for run/event failure analysis > Added a diagnostics tool that inspects either live run-store data or exported debug JSON, computes failure-signal aggregates (`search/browse/extraction/semantic/retrieval`), summarizes stop reasons/tool errors/slow calls, and returns actionable recommendations for faster debugging.
- **2026-03-08** > src/tools/search/providers/brightDataProvider.ts, src/config/env.ts, src/gateway/app.ts, .env.example, tests/unit/brightDataProvider.test.ts > Bright Data fallback contract fix (POST `/request` + `zone`) > Fixed Bright Data integration bug causing `404 Not found` by switching provider calls to Bright Data `/request` POST payload format (`zone`, `url`, `method`, `format`), adding required zone/country env wiring, parsing nested `body`/`response` JSON payloads, and only enabling Bright Data fallback when both API key and zone are configured.
- **2026-03-08** > src/tools/search/providers/brightDataProvider.ts, src/gateway/app.ts, src/config/env.ts, src/types.ts, .env.example, tests/unit/{brightDataProvider,searchManager}.test.ts > Bright Data fallback search provider wiring > Added a new `brightdata` search provider with configurable endpoint/path/engine/timeouts, generic JSON result normalization, and error-body diagnostics; wired gateway fallback preference to `brightdata` (when key is present) before `brave`; added new env variables and unit coverage.
- **2026-03-08** > src/core/runReActLoop.ts, src/core/runAlfredOrchestratorLoop.ts, src/prompts/master/alfred.system.ts, tests/smoke/reactLoop.test.ts > Alfred runtime separation (master orchestrator delegates specialist runtime) > Wired `runReActLoop` to run through `runAlfredOrchestratorLoop` so Alfred now owns turn-level planning/delegation, delegates lead execution to `lead_agent`, and evaluates/refines based on specialist output with budget/safety guardrails; added orchestration smoke coverage and kept Alfred persona instructions in the master prompt.
- **2026-03-08** > src/agent/tools/definitions/{webFetch,emailEnrich}.tool.ts, src/agent/toolPolicies.ts, src/agent/tools/registry.ts, src/agent/types.ts, src/core/runLeadAgenticLoop.ts, src/core/runReActLoop.ts, tests/unit/agentToolRegistry.test.ts > standalone fetch/enrich tools + per-agent tool allowlist injection > Added standalone `web_fetch` and `email_enrich` tools (while keeping `lead_pipeline` compatibility), introduced runtime lead-agent tool allowlist injection, extended lead-agent state/context with fetched-page scratch storage, and improved planner/observation summaries to understand the new tools.
- **2026-03-07** > docs/agent_separation.md, docs/spec.md, src/tools/lead/schemas.ts, src/tools/lead/subReactPipeline.ts, src/core/runLeadAgenticLoop.ts, src/prompts/domains/leadGeneration.system.ts, src/prompts/roles/queryExpansion.system.ts, src/agent/tools/definitions/leadPipeline.tool.ts, tests/unit/leadSchemas.test.ts > Alfred-vs-LeadGen boundary contract + objective-driven generic lead pipeline > Documented explicit master/specialist separation (`Alfred` orchestrator vs `LeadGenAgent` specialist), added lead objective-brief schema and telemetry in query expansion, removed SI/MSP-specific fallback/query/extraction assumptions in favor of user-objective-driven behavior, and updated planner/fallback phrasing/tool metadata to keep lead generation vertical-agnostic.
- **2026-03-07** > src/prompts/composePrompt.ts, src/prompts/master/alfred.system.ts, src/prompts/domains/leadGeneration.system.ts, src/prompts/roles/{planner,queryExpansion,extractor}.system.ts, src/core/runLeadAgenticLoop.ts, src/tools/lead/subReactPipeline.ts, src/agent/types.ts, src/core/runReActLoop.ts, src/agent/tools/definitions/{fileList,fileRead,fileWrite,fileEdit,shellExec,processList,processStop}.tool.ts, src/agent/tools/helpers/pathSafety.ts, tests/unit/agentToolRegistry.test.ts, tests/unit/promptComposer.test.ts > runtime prompt-stack composition, tool-context policy/root wiring, auto-discovered local ops tools > Added runtime-composed Alfred persona/domain/role prompt stack (with versioned prompt metadata emitted in timeline events), propagated policy mode + project-root context into tool execution, and exposed new guarded file/shell/process tools (`file_list`, `file_read`, `file_write`, `file_edit`, `shell_exec`, `process_list`, `process_stop`) through the auto-discovered agent tool registry with unit coverage.
- **2026-03-07** > src/services/openAiClient.ts, tests/unit/openAiClient.test.ts > GPT-5 temperature compatibility > Fixed OpenAI client incompatibility for `gpt-5-mini` by omitting explicit `temperature` in chat/structured requests for GPT-5-family models, and added unit coverage to verify temperature omission for GPT-5 and retention for non-GPT-5 models.
- **2026-03-07** > src/services/openAiClient.ts > `runOpenAiChat`, `runOpenAiStructuredChatWithDiagnostics` default model selection > Switched Alfred's default OpenAI model from `gpt-4.1-mini` to `gpt-5-mini` for chat and structured JSON-schema calls when no explicit model override is provided.
- **2026-03-07** > src/core/runLeadAgenticLoop.ts, tests/unit/plannerFailureGuardrails.test.ts, tests/unit/budgetGuardrails.test.ts, docs/progress.md > adaptive outer-loop guardrails + semantic failure telemetry > Added non-prescriptive failure-code signaling (`semantic_miss`, `search_retrieval_blocked`, `low_email_coverage`, `llm_budget_exhausted`) into observation/planner context, introduced novelty guardrails to avoid repeating near-identical low-yield `lead_pipeline` actions, added adaptive iteration ceiling logic based on budget/yield, and enforced a post-recovery yield attempt before diminishing-returns stop; expanded unit coverage for the new guardrails/signals.
- **2026-03-07** > src/tools/lead/subReactPipeline.ts, src/agent/tools/definitions/leadPipeline.tool.ts, tests/unit/emailEnrichment.test.ts, docs/progress.md > quick-scan email enrichment + duplicate-email quality guardrails > Added a two-phase enrichment flow that quick-scans contact/home pages with Cheerio before Playwright fallback, tightened email acceptance to business/source-related domains, scrubbed duplicate emails across candidate leads, and exposed quick-scan telemetry in tool/timeline outputs with new unit coverage.
- **2026-03-07** > src/core/runLeadAgenticLoop.ts, src/config/env.ts, .env.example, src/tools/lead/browserPool.ts, docs/progress.md > planner low-yield diversification + default budget/nav tuning > Updated planner instructions to require explicit strategy shifts on low-yield replans and avoid duplicate query repetition, increased default agent test budgets (`ALFRED_AGENT_MAX_DURATION_MS=600000`, `ALFRED_AGENT_PLANNER_MAX_CALLS=10`), and switched browse navigation to `domcontentloaded` first with `networkidle` fallback for faster page acquisition.
- **2026-03-06** > src/tools/search/searchManager.ts, src/agent/tools/definitions/searchStatus.tool.ts, src/core/runLeadAgenticLoop.ts, tests/unit/searchManager.test.ts, tests/unit/plannerFailureGuardrails.test.ts > recovery diagnostics + planner-visible outage context > Added detailed primary-recovery telemetry (start command, exit code/signal, stderr/stdout snippets, spawn/runtime errors, timestamps), exposed `lastPrimaryRecovery` via `search_status`, improved planner note summaries to include recovery failure details, and counted direct `search` tool errors as search-failure signals for smarter replanning/stop decisions.
- **2026-03-06** > .gitignore > ignore patterns > Removed root `debug/` from gitignore so exported run debug files can be tracked/shared when needed.
- **2026-03-06** > .gitignore > ignore patterns > Added root `debug/` to gitignore so local exported run-debug bundles do not appear as untracked workspace noise.
- **2026-03-06** > src/core/runLeadAgenticLoop.ts, src/agent/tools/definitions/leadPipeline.tool.ts, src/tools/lead/subReactPipeline.ts, tests/unit/leadFilters.test.ts > `softStopRemainingMs` flow and extraction/email soft-stop behavior > Added mode-derived soft-stop budget hints to `lead_pipeline` calls, passed them through tool input into sub-pipeline execution, and made extraction/email stages stop queued work early under low remaining budget while still returning partial leads with explicit `stoppedEarlyReason` telemetry.
- **2026-03-06** > src/core/runLeadAgenticLoop.ts, tests/unit/budgetGuardrails.test.ts, tests/unit/plannerFailureGuardrails.test.ts > `computeDeficitStrategy`, `computeExpectedLlmCapForIteration`, `computeYieldPerTokenSignal`, planner context/prompt wiring > Added deficit-aware planning strategy (`growth` vs `polish_only` with mode thresholds), mode-tied per-iteration LLM cap planning with high-yield reward and budget-exhaustion penalty, and rolling yield-per-1k-token guardrail signals passed into planner context/events plus unit coverage.
- **2026-03-06** > src/core/runLeadAgenticLoop.ts, tests/unit/budgetGuardrails.test.ts, tests/unit/plannerFailureGuardrails.test.ts > `minLeadPipelineStartMsForMode`, planner low-time guidance, observation token tracking > Replaced fixed lead-pipeline start guardrail with budget-mode thresholds (`normal=30s`, `conserve=15s`, `emergency=5s`), added planner instruction to prefer lightweight/stop actions when `<60s` remain, and completed observation/test wiring for per-action LLM token tracking.
- **2026-03-03** > package.json, tsconfig.json, .env.example, .gitignore > `scripts`, compiler config, env contract > Bootstrapped the Alfred MVP TypeScript project with runnable gateway/test scripts, strict config, and environment-driven runtime settings.
- **2026-03-03** > src/core/runReActLoop.ts, src/services/chatService.ts, src/core/approvalPolicy.ts > `runReActLoop`, `handleTurn`, `evaluateApprovalNeed` > Implemented the core ReAct execution loop, async/inline turn handling, and policy-aware approval gating.
- **2026-03-03** > src/gateway/app.ts, src/gateway/server.ts > route handlers for `/v1/sessions`, `/v1/chat/turn`, `/v1/runs`, `/v1/runs/:runId`, `/v1/runs/:runId/export`, `/v1/providers/status` > Added Hono gateway APIs for sessions, chat turns, run timelines, provider health, and redacted debug export.
- **2026-03-03** > src/tools/search/searchManager.ts, src/tools/search/providers/searxngProvider.ts, src/tools/search/providers/braveProvider.ts > `search`, `getProviderStatus`, `ensurePrimaryHealthy` > Added SearXNG-primary search with healthcheck/auto-start and Brave fallback, including a hard max of 15 search results for MVP.
- **2026-03-03** > src/tools/scrape/cheerioExtractor.ts, src/tools/scrape/playwrightExtractor.ts, src/tools/lead/leadPipeline.ts, src/tools/csv/writeCsv.ts > `scrapeLeadCandidatesWithCheerio`, `scrapeLeadCandidatesWithPlaywright`, `buildLeadCandidates`, `writeLeadsCsv` > Implemented Cheerio-first lead extraction from top search results, optional Playwright enrichment, and CSV artifact generation.
- **2026-03-03** > src/memory/sessionStore.ts, src/memory/dailyNotes.ts, src/runs/runStore.ts, src/workers/inMemoryQueue.ts > session CRUD, daily-note append, run/event persistence, in-memory queue scheduling > Added session persistence, Markdown daily transcript logging, JSON run state + JSONL event traces, and local async queue execution.
- **2026-03-03** > webui/index.html, webui/app.js, webui/styles.css > UI session controls, run execution, timeline loading, debug export trigger > Added a usable web console for running Alfred turns and exporting diagnostic bundles.
- **2026-03-03** > tests/unit/*.test.ts, tests/integration/*.test.ts, tests/smoke/*.test.ts, tests/security/*.test.ts > search fallback coverage, approval policy checks, session integration, smoke loop run, redaction checks > Added foundational automated tests spanning unit, integration, smoke, and security scopes.
- **2026-03-03** > docs/progress.md > phase map and completion tracking > Added project progress tracking mapped directly to the approved implementation phases.
- **2026-03-03** > src/core/runReActLoop.ts, src/tools/scrape/cheerioExtractor.ts, src/tools/lead/leadPipeline.ts, tests/unit/cheerioExtractor.test.ts > `buildSearchQueries`, `parseRequestedLeadCount`, `scrapeLeadCandidatesWithCheerio`, `buildLeadCandidates` > Improved lead quality by expanding search into multi-query fan-out, extracting company entries from list/directory pages (instead of page-title-only candidates), ranking/filtering candidates, and adding a regression test for list-style extraction quality.
- **2026-03-03** > src/core/runReActLoop.ts, src/tools/lead/subReactPipeline.ts, src/tools/lead/browserPool.ts, src/tools/lead/schemas.ts, src/tools/lead/llmBudget.ts > `runReActLoop`, `executeLeadSubReactPipeline`, `BrowserPool.collectPages`, `LlmBudgetManager.consume` > Replaced deterministic lead extraction with a sub-ReAct pipeline that performs LLM-driven query expansion, Playwright page browsing with a persistent browser/context pool, batched structured extraction, and confidence-based quality gating.
- **2026-03-03** > src/types.ts, src/tools/csv/writeCsv.ts > `LeadCandidate`, `RunPhase`, `writeLeadsCsv` > Migrated to strict lead output schema (`companyName`, `website`, `location`, `shortDesc`, `sourceUrl`, `confidence`, `evidence`) and added explicit `sub_react_step` run timeline phase support.
- **2026-03-03** > src/config/env.ts, .env.example, package.json, scripts/setup-playwright.js > env parsing, setup scripts, browser bootstrap > Added sub-ReAct runtime config knobs (max pages, batch size, LLM cap, confidence threshold) and one-time Playwright browser setup workflow with `setup:browsers` and guarded `postinstall`.
- **2026-03-03** > src/tools/scrape/cheerioExtractor.ts, src/tools/scrape/playwrightExtractor.ts, src/tools/lead/leadPipeline.ts, tests/unit/cheerioExtractor.test.ts > legacy extraction modules removed > Removed the prior heuristic-only Cheerio/legacy pipeline path in favor of the new hard-replaced sub-ReAct extraction flow.
- **2026-03-03** > docs/to_revisit.md > deferred roadmap items > Added explicit follow-up notes for user-configurable browse budgets, higher LLM call caps, and future full top-level ReAct migration.
- **2026-03-03** > src/core/runReActLoop.ts, webui/app.js > summary budget accounting and timeline rendering > Counted summary LLM call usage against run-level budget metrics and improved run timeline UI readability for `sub_react_step` phases while keeping raw JSON visible.
- **2026-03-04** > package.json, pnpm-lock.yaml, package-lock.json, scripts/test-unit, scripts/test-integration, scripts/test-smoke, scripts/test-security, scripts/setup-playwright.js, scripts/test-manual-checklist, docs/progress.md > package-manager scripts, lockfile, and cleanup > Migrated repository workflow from npm to pnpm by switching script wrappers/commands, adding pnpm package-manager metadata, adding `pnpm-lock.yaml`, and removing `package-lock.json`.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, src/services/openAiClient.ts, src/tools/lead/schemas.ts, src/tools/lead/requestIntent.ts, tests/unit/requestIntent.test.ts > `buildQueryPlan`, `extractBatch`, `runOpenAiStructuredChatWithDiagnostics`, `parseRequestedLeadCount` > Fixed requested lead-count intent handling (including prompts like “find me 20 …”) by combining model-planned target count with stronger fallback parsing, and added per-batch extraction failure diagnostics (`failureReasons`) to run timeline events for actionable debugging.
- **2026-03-04** > src/services/openAiClient.ts, src/tools/lead/subReactPipeline.ts, tests/unit/openAiClient.test.ts > `runOpenAiStructuredChatWithDiagnostics`, `buildHttpErrorDetails`, `executeLeadSubReactPipeline` > Added sanitized OpenAI HTTP error diagnostics (type/code/message/request-id/retry-after/rate-limit headers), propagated planner failure details into `query_expansion` timeline payloads, and added structured extraction failure detail payloads per batch for faster run debugging.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, src/tools/lead/schemas.ts, tests/unit/leadSchemas.test.ts > `QUERY_EXPANSION_JSON_SCHEMA`, `EXTRACTED_LEAD_BATCH_JSON_SCHEMA`, `executeLeadSubReactPipeline` > Fixed strict response-format schema compatibility by requiring all JSON-schema keys with nullable optional fields (`targetLeadCount`, `website`, `location`) and added per-query search success/failure telemetry in `browse_batch` events so zero-URL runs expose provider errors.
- **2026-03-04** > src/services/chatService.ts, webui/app.js > `executeRun`, live run polling helpers > Added backend heartbeat events (`observe:heartbeat`) while runs are executing, and upgraded UI run handling to default async mode with live polling so users see ongoing progress messages and timeline updates during long-running pipelines.
- **2026-03-09** > src/core/runAlfredOrchestratorLoop.ts, tests/unit/runAlfredOrchestratorLoop.test.ts, tests/smoke/reactLoop.test.ts > `runAlfredOrchestratorLoop`, `runCompletionEvaluator` > Added an explicit Alfred completion-evaluator pass after successful tool/delegation results, fed evaluator notes back into planner context as reflection hints, and added orchestration unit coverage so Alfred can switch from gather mode to final response without looping blindly after a sufficient result.
- **2026-03-09** > src/core/runLeadAgenticLoop.ts, tests/unit/plannerFailureGuardrails.test.ts, docs/progress.md > `buildReflectionHints`, planner context payload, `agent_plan_created` > Removed lead-loop action rewrites (`applyFailureGuardrail`, `applyNoveltyGuardrail`) and replaced them with planner-visible reflection hints derived from recent failures/semantic misses, keeping only generic query-sanity rails while preserving observability and unit coverage.
- **2026-03-09** > src/agent/skills/types.ts, src/agent/skills/registry.ts, src/agent/skills/leadAgentSkill.ts, src/core/runAgentLoop.ts, src/core/runAlfredOrchestratorLoop.ts, tests/unit/agentSkillRegistry.test.ts > skill registry + `runAgentLoop` delegation > Introduced a generic skill registry/dispatcher layer, registered `lead_agent` as a first-class skill with its own metadata and tool allowlist, and updated Alfred orchestration to discover/delegate skills through `runAgentLoop` instead of hardcoding the lead runtime import path.
- **2026-03-09** > src/agent/skills/types.ts, src/core/runAlfredOrchestratorLoop.ts, src/core/runLeadAgenticLoop.ts, tests/unit/leadAgentDelegationTelemetry.test.ts > delegation telemetry + scratchpad scaffold > Added lightweight parent/delegation metadata (`parentRunId`, `delegationId`) and scratchpad passing to skill runtime options, logged delegation start/result events in Alfred, and surfaced scratchpad keys on lead-agent loop start as the first scaffold for future multi-agent shared memory.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, src/tools/lead/schemas.ts, src/types.ts, src/tools/csv/writeCsv.ts, src/core/runReActLoop.ts, tests/unit/leadSchemas.test.ts, tests/smoke/reactLoop.test.ts > `qualityGate`, `ExtractedLeadSchema`, `LeadCandidate`, `writeLeadsCsv` > Added employee-size extraction fields and soft size-match scoring (`in_range`, `near_range`, `unknown`, `out_of_range`), implemented deficit-triggered relax mode (`0.55` floor for size-filtered requests), propagated size-match telemetry to run outputs/timeline, and expanded CSV output with size/selection metadata.
- **2026-03-04** > src/tools/lead/schemas.ts, src/tools/lead/subReactPipeline.ts, src/types.ts, src/tools/csv/writeCsv.ts, tests/unit/leadSchemas.test.ts > `ExtractedLeadSchema`, `buildExtractionPrompt`, `LeadCandidate`, `writeLeadsCsv` > Hotfixed extraction schema rigidity by renaming `sizeSource` to flexible `sizeEvidence` text (non-URL), updating prompts/CSV/output types accordingly, and removing URL-format validation that was causing batch-wide Zod extraction failures.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts > `EXTRACTION_SYSTEM_PROMPT`, `buildExtractionPrompt`, extraction retry system message > Replaced extraction instructions with a stronger schema-aligned system prompt covering batched pages, anti-hallucination constraints, explicit min/max parsing guidance, near-range confidence rules, strict `{ leads: [...] }` output contract, and schema-repair retry guidance.
- **2026-03-04** > src/agent/types.ts, src/agent/tools/registry.ts, src/agent/tools/definitions/*.tool.ts, tests/unit/agentToolRegistry.test.ts > `discoverLeadAgentTools`, tool definition schemas/metadata > Added pass-2 agent infrastructure with auto-discoverable tool registry and Zod-described tool definitions (`lead_pipeline`, `search`, `write_csv`) as the foundation for model-selected plan/act tool execution.
- **2026-03-04** > src/core/runLeadAgenticLoop.ts, src/core/runReActLoop.ts > `runLeadAgenticLoop`, planner/action loop, `agent_stop` events > Implemented lead-only outer agent loop (plan -> act -> observe -> replan) with model/fallback planning, single+parallel tool actions, diminishing-returns checks, budget/time stop conditions, and explicit `agent_stop` reasons while preserving existing `/v1/chat/turn` API wiring.
- **2026-03-04** > src/config/env.ts, .env.example, src/gateway/app.ts, src/services/chatService.ts > agent budget config parsing + wiring > Added `.env` controls for agent-loop tuning (`ALFRED_AGENT_MAX_DURATION_MS`, `ALFRED_AGENT_MAX_TOOL_CALLS`, `ALFRED_AGENT_MAX_PARALLEL_TOOLS`, `ALFRED_AGENT_PLANNER_MAX_CALLS`, `ALFRED_AGENT_OBSERVATION_WINDOW`, `ALFRED_AGENT_DIMINISHING_THRESHOLD`) and passed them through gateway/service into runtime.
- **2026-03-04** > src/core/runLeadAgenticLoop.ts > planner structured-output contract, `parseToolInputJson`, `fallbackPlan` > Fixed planner 400 schema failures by switching tool inputs to stringified `inputJson` objects (strict JSON-schema compatible), parsing/validating them before execution, and refining fallback sequencing to avoid repeated `write_csv` loops when planner calls fail.
- **2026-03-04** > src/tools/search/searchManager.ts, src/agent/tools/definitions/searchStatus.tool.ts, src/agent/tools/definitions/recoverSearch.tool.ts, src/core/runLeadAgenticLoop.ts, src/tools/lead/subReactPipeline.ts, src/agent/tools/definitions/leadPipeline.tool.ts > `recoverPrimary`, `search_status`, `recover_search`, planner system guidance, pipeline search-failure telemetry > Added agentic search-service recovery: the planner can inspect provider health, attempt primary-provider restart, and then retry lead/search actions; search manager now retries primary after recovery attempts and exposes richer provider diagnostics, while pipeline/tool outputs surface search-failure counts for evidence-based replanning.
- **2026-03-04** > src/types.ts, src/tools/lead/schemas.ts, src/tools/lead/subReactPipeline.ts, src/tools/csv/writeCsv.ts, tests/unit/leadSchemas.test.ts > `LeadCandidate.email`, extraction schema/prompt normalization, CSV headers > Added first-class email support to lead extraction/output by extending lead types and structured extraction contract with `email`, normalizing extracted emails safely, and exporting `email` in lead CSV artifacts.
- **2026-03-04** > src/runs/runStore.ts, src/services/chatService.ts, src/core/runReActLoop.ts, src/core/runLeadAgenticLoop.ts, src/gateway/app.ts, webui/index.html, webui/app.js, tests/integration/runsCancelApi.test.ts > run cancellation API + cooperative loop checks + UI control > Implemented `POST /v1/runs/:runId/cancel` with cancellation-request persistence, cooperative cancellation checks in the outer loop/sub-pipeline, partial-result finalization (`cancelled` status + artifact write), and a new UI `Cancel Run` button for stopping long runs without losing collected leads.
- **2026-03-04** > src/core/runLeadAgenticLoop.ts, src/tools/lead/subReactPipeline.ts, src/tools/lead/browserPool.ts, src/agent/tools/definitions/leadPipeline.tool.ts, tests/smoke/reactLoop.test.ts > lead dedupe keying, browse fallback/telemetry, tool output diagnostics > Improved lead-quality reliability by fixing dedupe collisions (no longer collapsing multiple companies from a single aggregator `sourceUrl` domain when website is missing), adding Playwright navigation fallback (`networkidle` -> `domcontentloaded`) with per-URL failure diagnostics, and surfacing browse-failure counts/samples into timeline/tool outputs for debugging low-yield runs.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts > `EXTRACTION_SYSTEM_PROMPT` employee-size parsing and evidence rules > Updated extraction instructions to enforce stricter employee range parsing (`employeeMin`/`employeeMax`) with conservative null fallback, explicit anti-aggregator-name constraints, and payload-grounded schema guidance while retaining first-class `email` extraction.
- **2026-03-04** > .env.example > `SEARXNG_START_CMD` default startup hint > Set `SEARXNG_START_CMD=./manage webapp.run` in the sample env to make local primary-search auto-recovery usable out-of-the-box when SearXNG is managed via the repository helper command.
- **2026-03-04** > src/tools/lead/schemas.ts, src/tools/lead/subReactPipeline.ts, src/types.ts, src/tools/csv/writeCsv.ts, tests/unit/leadSchemas.test.ts > extraction schema contract (`employeeSizeText`, `emailEvidence`) and CSV export fields > Tightened extraction structure by requiring non-null `employeeSizeText` (`"unknown"` fallback), adding `emailEvidence` tracking, expanding extraction prompt guidance for email surface discovery, and exporting email evidence in the lead CSV.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, tests/unit/leadQualityScoring.test.ts > quality-gate size scoring (`near_range`, `unknown`) > Updated lead scoring so `unknown` size is neutral (no penalty) and `near_range` receives a small positive adjustment, then added unit coverage to verify ordering against `in_range` and `out_of_range`.
- **2026-03-04** > src/core/runLeadAgenticLoop.ts, tests/unit/agentMinConfidence.test.ts > adaptive `minConfidence` defaults across replans > Added iteration-aware defaulting for `lead_pipeline.minConfidence` (iteration 1 = 0.70, iteration 2 = 0.65, iteration 3+ = 0.60 when deficit remains high), applying these defaults automatically when planner/fallback actions omit the field.
- **2026-03-04** > src/tools/lead/filters.ts, src/agent/tools/definitions/leadPipeline.tool.ts, src/tools/lead/subReactPipeline.ts, tests/unit/leadFilters.test.ts > filter wiring (`filters`) from planner -> tool -> sub-pipeline > Added explicit lead-pipeline filter schema/normalization (`employeeCountMin/Max`, `country`, `industryKeywords`, `requireEmail`), preserved planner-provided filters instead of dropping them at input parsing, and applied those filters to query expansion and target employee-range resolution.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, src/agent/tools/definitions/leadPipeline.tool.ts, tests/unit/emailEnrichment.test.ts > `email_enrichment` browse pass + output telemetry > Added a post-extraction email enrichment step that revisits company websites (`/`, `/contact`, `/contact-us`, `/about`), extracts/normalizes business emails with source evidence, surfaces enrichment metrics in timeline/tool output, and added unit coverage for payload email parsing/selection.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, tests/unit/leadQualityScoring.test.ts > email-aware quality scoring > Updated quality scoring to include explicit email bonus/penalty so no-email leads rank lower (especially when email is requested), and added unit coverage confirming email-present leads score higher under email-required scenarios.
- **2026-03-04** > src/core/runLeadAgenticLoop.ts > deterministic run summary metrics (`assistantText`, final payload) > Replaced LLM-generated end-of-run narration with deterministic computed metrics (lead count, deficit, email coverage, observed failure counts, stop reason, call budgets, elapsed time) and extended final event payload with `emailLeadCount`/`emailCoverageRatio` to keep UI/reporting consistent with actual run data.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, src/core/runLeadAgenticLoop.ts, tests/unit/leadDedupe.test.ts > company-identity-first dedupe keying > Hardened dedupe to prioritize normalized company identity (`companyName` + normalized location, with suffix stripping) before URL domain fallback so profile/listing URLs no longer create duplicate leads for the same company across sub-pipeline and outer-loop merges.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, src/agent/tools/definitions/leadPipeline.tool.ts, src/core/runLeadAgenticLoop.ts, tests/unit/plannerFailureGuardrails.test.ts > failure-aware planning signals + search-failure guardrail > Added extraction-failure telemetry to lead-pipeline outputs (`extractionFailureCount` and samples), upgraded planner context with structured failure signals, and introduced a deterministic guardrail that forces `search_status` before retrying `lead_pipeline` after search-failure iterations.
- **2026-03-04** > src/core/runLeadAgenticLoop.ts, src/agent/types.ts, src/agent/tools/definitions/leadPipeline.tool.ts, src/tools/lead/subReactPipeline.ts, src/tools/lead/browserPool.ts, tests/unit/budgetGuardrails.test.ts > deadline-aware graceful shutdown + time-budget guardrails > Added global deadline propagation into tool context/sub-pipeline, introduced pre-action budget guardrails to avoid starting expensive lead passes when budget is too low, downscaled lead-pipeline crawl settings under tight remaining time, added timeout-aware browser collection behavior, and ensured runs stop gracefully with partial results when deadline is exhausted.
- **2026-03-04** > src/tools/lead/subReactPipeline.ts, src/agent/tools/definitions/leadPipeline.tool.ts, tests/unit/emailEnrichment.test.ts > budget-aware email enrichment caps + diminishing-return stop > Added deadline-sensitive URL caps for enrichment crawl depth, introduced early-stop behavior when enrichment yields diminish, propagated enrichment cap/stop-reason telemetry to tool outputs/events, and covered cap behavior in unit tests.
- **2026-03-04** > src/services/chatService.ts > `executeRun` heartbeat timer > Reduced run heartbeat emission frequency from every 10 seconds to every 30 seconds to cut timeline noise while preserving live progress visibility.
- **2026-03-04** > src/services/openAiClient.ts, src/tools/lead/subReactPipeline.ts, src/core/runLeadAgenticLoop.ts, src/runs/runStore.ts, src/types.ts, src/agent/tools/definitions/leadPipeline.tool.ts, tests/unit/openAiClient.test.ts, tests/unit/runStoreUsage.test.ts, tests/smoke/reactLoop.test.ts > LLM usage capture/aggregation (`usage`, `llmUsage`) and failure-aware planner context (`aggregateFailures`) > Added token-usage capture from OpenAI structured responses, propagated usage through sub-pipeline and tool outputs, persisted per-run usage totals in run state/events/final summary, and strengthened planner prompts/context to explicitly react to accumulated failure signals.
- **2026-03-04** > webui/app.js, docs/progress.md > timeline rendering helpers (`summarizeEvent`, `formatTokenUsage`, verbose extraction/email failure snippets) > Upgraded Run Timeline readability with explicit planner-thought summaries, per-step failure detail snippets, `observe:llm_usage` deltas, and run-level token usage headers so active runs expose meaningful behind-the-scenes progress without relying on raw JSON.
- **2026-03-04** > webui/app.js > `renderTimelineView` thought-first cleanup > Simplified timeline output by removing expanded planner `action` blocks and removing the trailing raw JSON dump, keeping the top timeline concise and focused on thought/progress signals.
- **2026-03-05** > src/core/runLeadAgenticLoop.ts, tests/unit/budgetModes.test.ts > budget-mode state (`normal|conserve|emergency`), hysteresis helpers, planner budget prompt/context, budget-mode events > Added dynamic budget-mode switching with hysteresis, budget snapshots passed into planner context, low-budget planning guidance in planner prompt, and timeline events when budget mode changes at pre-plan/pre-action/post-action checkpoints.
- **2026-03-05** > src/core/runLeadAgenticLoop.ts, tests/unit/plannerFailureGuardrails.test.ts > planner context memory (`pastActionsSummary`) and action-note summarization > Added capped past-action summaries (recent iteration outcomes + key tool inputs) to planner context so the model can avoid repeating low-yield actions without prompt bloat, and added unit coverage for summary size/recency constraints.
- **2026-03-05** > src/core/runLeadAgenticLoop.ts, tests/unit/budgetGuardrails.test.ts > mode-dependent shell bounds (`leadPipelineBoundsForMode`) and reduced hard-start threshold > Switched deterministic shell controls to budget-mode-aware clamps for `lead_pipeline` inputs (`normal/conserve/emergency`) and lowered the hard start threshold to 20s so low-budget runs can still execute constrained passes instead of hard-stopping too early.
- **2026-03-05** > src/core/runLeadAgenticLoop.ts, src/agent/tools/definitions/leadPipeline.tool.ts, src/tools/lead/subReactPipeline.ts, tests/unit/leadFilters.test.ts > planner-controlled email enrichment (`runEmailEnrichment`) + low-budget strategy hints > Added an explicit planner-controlled `runEmailEnrichment` input on `lead_pipeline`, wired it through to sub-pipeline execution so enrichment can be intentionally skipped under constrained/high-deficit conditions, and updated planner prompt/context hints to keep this decision agent-driven.
- **2026-03-05** > src/core/runLeadAgenticLoop.ts, webui/app.js > final budget snapshot telemetry and summary rendering > Added explicit budget snapshot metrics (mode + remaining time/tool/planner/LLM ratios and counts) to final run events and deterministic assistant summary, and surfaced that snapshot in timeline stop lines for clearer run-end diagnostics.
- **2026-03-05** > src/core/runLeadAgenticLoop.ts, tests/unit/plannerFailureGuardrails.test.ts > yield-aware diminishing-returns + planner performance summary context > Fixed stop logic so diminishing-returns only evaluates yield-relevant actions (ignoring diagnostic-only steps like `search_status`), and added compact `recentPerformanceSummary` metrics in planner context to improve replanning decisions after low-yield runs.
- **2026-03-05** > src/tools/search/searchManager.ts, src/config/env.ts, src/gateway/app.ts, src/agent/tools/definitions/searchStatus.tool.ts, src/tools/lead/subReactPipeline.ts, tests/unit/searchManager.test.ts, .env.example > search resiliency and diagnostics telemetry > Hardened SearXNG handling with healthcheck retries + short grace window after recent healthy checks, added structured `SearchManagerError` stage diagnostics (`healthcheck|primary_search|primary_retry|fallback_search`), surfaced provider failure telemetry in `search_status` and sub-ReAct search-failure samples, and added env knobs for retry/grace tuning.
- **2026-03-05** > src/core/runLeadAgenticLoop.ts, tests/unit/plannerFailureGuardrails.test.ts > `search` query guardrail (`applySearchQueryGuardrail`) > Added deterministic planner-action sanitization that auto-fills missing/invalid `search.query` inputs from the user request (instead of executing invalid tool calls), emits explicit `agent_plan_adjusted` telemetry, and adds unit coverage for query auto-fill behavior.
- **2026-03-05** > README.md, docs/progress.md > project overview, status snapshot, quickstart run/test docs > Added a root README covering Alfred's current scope, high-level plan, and pnpm-first local startup/testing instructions, and synchronized progress tracking with a documentation milestone note.
- **2026-03-05** > README.md, assets/alfred_masthead.jpg, assets/alfred_at_work.mp4, docs/progress.md > README masthead branding assets > Added new `assets/` branding media, wired the masthead image into the top of README, and updated progress tracking for the documentation/branding update.
- **2026-03-05** > README.md, docs/progress.md > long-term product vision framing in README > Updated README to describe Alfred's long-term second-brain vision and position lead generation as the intentional focused starting use case for measurable value and iteration.
- **2026-03-05** > README.md, docs/progress.md > README positioning refresh > Updated README intro copy to foreground the Alfred/Batman framing while preserving the long-term second-brain vision and focused lead-generation entry point.
