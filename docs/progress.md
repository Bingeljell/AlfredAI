# Alfred MVP Progress

## Phase Map

- Phase 0: Bootstrap and governance
- Phase 1: Gateway + sessions + run lifecycle
- Phase 2: ReAct loop orchestration
- Phase 3: Search provider manager (SearXNG + Brave fallback)
- Phase 4: Lead extraction pipeline (Cheerio-first, Playwright-enrichment)
- Phase 5: Web UI + debug export
- Phase 6: Policy modes (dev trusted / prod balanced)
- Phase 7: Hardening and documentation

## Current Status

- 2026-03-22: **Browser Reuse & Timeout Hardening.** Refactored `lead_extractor` and `lead_generation` to share a single browser instance per batch, significantly reducing latency. Added a 45s safety buffer to the lead generation loop to prevent ungraceful turn timeouts. Expanded the directory blacklist. Currently at 17 high-quality leads for the Texas MSP/SI vertical.

- 2026-03-22: **Bugfix + Lead Generation Skill.** Implemented `lead_generation` as a high-level skill (composite tool) and refactored `lead_extractor` for deep extraction. Fixed a bug in `agentLoop.ts` where `llmProviders` was missing from `ToolContext`, which was breaking the new deep extraction pipeline. Standardized token usage reporting across tools.

- 2026-03-21: **Workspace cleanup — remove per-turn knowledge extraction, unify artifact paths.** Deleted `dailyNotes.ts` (per-turn turn logging, superseded by `groupChatStore`) and `sessionExtractor.ts` (fired an extra LLM call after every turn, producing up to 100 fragmented Research/Lead/Decision/Project markdown files per day with marginal value). Removed both call sites from `runReActLoop.ts`. Changed `writer_agent` default artifact path from `sessions/{sessionId}/outputs/` to `sessions/{sessionId}/artifacts/{runId}-{format}.md` — all session outputs now live under a single consistent path.

- 2026-03-21: **Memory architecture: conversation window + group chat logs + SOUL/AGENTS injection + interrupted run recovery.** Replaced the 8-turn snippet window with a proper 30-entry (15-turn) sliding conversation window stored in session memory and injected as real `user`/`assistant` message pairs into every run — gives Alfred full turn-by-turn continuity without amnesia, and the stable system prompt prefix is cacheable by Gemini implicit caching. Added `GroupChatStore` writing daily JSONL chat logs per channel key (`workspace/alfred/groups/{channelKey}/logs/YYYY/MM/YYYY-MM-DD.jsonl`) so group-level chat history is durable across sessions. Baked `SOUL.md` and `AGENTS.md` into the system prompt at module load (zero tool calls per run — identity and codebase conventions always present). Added `recoverInterruptedRuns()` to `RunStore` marking any `running`/`queued` runs as `failed` on server restart (prevents stale stuck runs after crashes). Added graceful HTTP shutdown with 3s hard-exit fallback and `SIGHUP` handler to `server.ts`. Fully rewrote `AGENTS.md` with correct post-refactor paths, efficiency principles (avoid `git diff`/`git log -p`, read in large chunks, act once you have enough context), and memory architecture documentation.

- 2026-03-20: **Operations tools & Writing Agent Refactor.** Added system tools for introspection and recovery: `run_diagnostics`, `recover_search`, and `search_status`. Refactored the `writer_agent` tool to drastically simplify its implementation, strictly adhering to the core philosophy of keeping things simple and executing only the required amount. The writer agent now focuses solely on generating user-facing drafts from provided context without over-engineered orchestration. To prevent context drift across consecutive writing tasks in a long session, `state.fetchedPages` was structurally bounded to a single run in `agentLoop.ts`.

- 2026-03-20: **Architecture restructure + self-awareness foundation.** Collapsed `runOrchestrator` (trivial pass-through) into `runReActLoop` directly. Unified `src/agent/tools/` and `src/tools/` into one `src/tools/` directory. Moved LLM adapters from `src/services/llm/` → `src/provider/`. Renamed `src/core/` → `src/runtime/`. Moved `src/services/chatService.ts` → `src/runner/chatService.ts`. Moved `reliability.ts` to `src/utils/` (pure utility). Deleted dead code: `orchestrator.ts`, `toolPolicies.ts`. Added ESLint layer enforcement (`eslint.config.mjs`, `pnpm run lint:layers`) — 12 zones, zero violations. Added `SOUL.md` (Alfred identity document) and updated `AGENTS.md` with Alfred codebase map and self-development guide. Updated `src/runtime/specialists.ts` with SELF-AWARENESS section and Alfred persona intro. `docs/agent_separation.md` superseded by `docs/tool_contract.md` (single-agent model). All docs updated to reflect new paths and architecture.

- 2026-03-18: **UI redesign + Telegram session continuity.** Full rewrite of `webui/index.html`, `styles.css`, and `app.js`. New 2-column layout (sidebar + chat hero). Debug drawer replaces permanent right inspector rail — slides in from right, hidden by default, contains Live / Telemetry / Status / Config tabs. Assistant messages now rendered as Markdown via `marked.js`. Session list shows Telegram-linked sessions with `TG · <label>` badge (fetched from new `GET /v1/channels` endpoint). Session creation moved to modal dialog. Artifacts bar appears above composer only when artifacts exist. Active run animates brand orb and send button. Added `getAll()` to `ChannelSessionStore` and `/v1/channels` API route.

- 2026-03-18: **Track 4: Telegram channel adapter.** Each Telegram chat maps to an isolated Alfred session stored in `workspace/sessions/channel-sessions.json`. Commands: `/newsession` (requires yes/no confirmation), `/label <name>` (injects channel context into Alfred's system prompt), `/status`. Long runs send "Working on it…" after 8s, then deliver full response + file attachments. Private bot enforcement via `TELEGRAM_ALLOWED_USER_IDS` allowlist in `.env`.

- 2026-03-18: **Track 0: Multi-provider LLM support.** Extended `LlmProvider` interface with `generateWithTools()` and unified tool-calling types (`LlmConversationMessage`, `LlmToolCall`, `LlmToolDef`). Added `AnthropicLlmProvider` (raw fetch, tool_use format), `GeminiLlmProvider` (raw fetch, functionCall format), `OllamaLlmProvider` (OpenAI-compatible shim). Added provider registry (`src/services/llm/registry.ts`) with `getActiveLlmProvider()` singleton. Extended `src/config/env.ts` with `ALFRED_LLM_PROVIDER`, `ALFRED_MODEL_FAST`, `ALFRED_MODEL_SMART` and provider API key vars. Migrated `agentLoop.ts`, `orchestrator.ts`, `sessionExtractor.ts`, and `specialists.ts` to use provider abstraction. Setting `ALFRED_LLM_PROVIDER=gemini` + `GOOGLE_GEMINI_API_KEY` now routes all LLM calls through Gemini with no code changes. Validated with Gemini Flash 2.5.

- 2026-03-18: **Track 1: QMD second brain.** Added `sessionExtractor.ts` (post-session LLM call writes structured Markdown notes per category to `knowledge/`). Added `rag_memory_query` tool (wraps `qmd query` CLI, graceful fallback if QMD not installed). Research, writing, and lead specialists now call `rag_memory_query` as step 0 before searching the web. Added `docs/roadmap.md` with the full expansion roadmap.

- 2026-03-18: **Big-bang rewrite — native tool-calling architecture.** Replaced the entire `runAlfredOrchestratorLoop` + specialist loop stack (~4,000 lines) with three new files: `orchestrator.ts` (classification router), `agentLoop.ts` (native OpenAI tool-calling loop), and `specialists.ts` (4 specialist configs). Added `runOpenAiToolCallWithDiagnostics` to `openAiClient.ts`. `runReActLoop.ts` simplified to call `runOrchestrator`. Deleted 8 source files and 9 stale test files. Build clean, no new test failures. Architecture documented in `docs/architecture/corebehaviour_refactor.md`.

- 2026-03-17: Intensified the "SEARCH STRATEGY" directives and implemented explicit search repetition detection in the orchestrator loop. Mechanical hints now escalate to "Efficiency Warnings" when the agent ignores phase transitions or repeats queries, preventing tactical stutter and ensuring progression to verification. Fixed a bug where legitimate tool calls were being hijacked by the general-task guard.
- 2026-03-17: Fixed a critical bug in the orchestrator guard that caused infinite search loops by hijacking all general-task actions. Added "SEARCH STRATEGY" directives to the planner to ensure concise, keyword-based queries and prevent lazy paragraph-style searching.
- 2026-03-17: Stabilized the March 17 refactor by removing the `research_agent` middleman and simplifying the `writer_agent` into a single-pass tool. Converted deterministic "Forced Phase Transitions" into "Reflection Hints" in both Alfred's loop and the specialist loop, restoring tactical autonomy to the agent while preserving the authority of the `TurnContract`. Fixed TypeScript compilation errors and updated unit tests to match the hint-based architecture.
- 2026-03-17: Made the first deep architectural cut in Slice 2: general tasks no longer delegate to `research_agent` at all. Alfred now keeps general execution in its own loop, hides `research_agent` from planner-visible agent catalogs for general-task turns, and reuses stored session artifacts via direct `file_read` when a follow-up already has a reusable body.
- 2026-03-17: Slice 2/3 moved forward in code: Alfred now hides `research_agent` from the planner for simple direct-execution research/list shapes, and specialist writer actions are normalized back to the immutable task contract before execution so explicit `blog_post` payloads can no longer silently drift ranked-list/list/comparison tasks into article mode.
- 2026-03-17: Slice 2 has started in code: Alfred now keeps simple general ranked-list/list/comparison/brief research tasks in its own loop by rewriting `research_agent` delegation into direct tool execution, reducing duplicate semantic planning for those task shapes.
- 2026-03-17: Added [docs/implementation/corebehaviour_execution_plan_17March.md](/Users/nikhil/Projects/Alfredv1/docs/implementation/corebehaviour_execution_plan_17March.md) as the surgical execution log for the refactor, mapping Slices 2–6 to exact files, functions, deletion targets, tests, risks, and exit criteria.
- 2026-03-17: Slice 1 of the March 17 refactor is now in progress in code: turn interpretation is authoritative for the canonical turn contract when available, delegated research task contracts preserve interpretation-owned semantics, and specialist fallback contracts no longer regex-infer draft/citation intent from raw prompt text.
- 2026-03-17: Added [docs/architecture/corebehaviour_refactor_plan_17March.md](/Users/nikhil/Projects/Alfredv1/docs/architecture/corebehaviour_refactor_plan_17March.md) as the replacement architecture plan to remove duplicate semantic control layers and move Alfred toward one turn contract, one execution loop, one semantic validator, and tools/skills that own mechanics rather than semantics.

- **Phase 0**: Completed
  - Project scaffold created with TypeScript scripts and `.env.example`.
  - Test script wrappers now map to real `pnpm run test:*` commands.
- **Phase 1**: Completed
  - Hono gateway, session APIs, run persistence and JSONL timeline logging implemented.
- **Phase 2**: Completed
  - `runReActLoop(sessionId, message, options)` implemented and wired to `/v1/chat/turn`.
- **Phase 3**: Completed
  - SearXNG provider with healthcheck + auto-start command + Brave fallback implemented.
  - Search results capped at max 15 in manager-level enforcement.
- **Phase 4**: Completed
  - Cheerio-based first pass on top results implemented.
  - Optional Playwright enrichment pass implemented as dynamic/optional dependency path.
- **Phase 5**: Completed
  - Simple web UI added with sessions list/create, chat turn execution, run timeline, and debug export.
- **Phase 6**: Completed
  - `ALFRED_ENV=dev|prod` policy behavior wired through approval decision logic.
- **Phase 7**: In progress
  - Core tests added (unit/integration/smoke/security).
  - Writer reliability hardening now blocks artifact quality downgrades: if a writer fallback/placeholder would overwrite an existing complete draft at the same `outputPath`, persistence is skipped with explicit `writer_stage` telemetry (`quality_downgrade_prevented`) and the existing complete draft is preserved as the effective output for that call.
  - Writer completion semantics are now stricter: incomplete drafts no longer get upgraded to `draftQuality=complete` via word-count-only fallback, reducing false-positive “complete” signals from partially failed passes.
  - Writer now fast-fails when synthesis evidence is missing: if no fetched source cards/context snippets are available, `writer_agent` skips multi-pass LLM calls, emits `insufficient_source_material` stage telemetry, and returns a bounded fallback draft immediately instead of burning timeout budget on low-evidence attempts.
  - Writer architecture now uses a provider-agnostic LLM layer (`LlmProvider` + fallback router) and a multi-pass topic-agnostic generation flow (plan -> section synthesis -> polish -> optional repair), so writing behavior can evolve to Gemini/Claude adapters later without rewriting the writer pipeline.
  - Writer timeout behavior now uses soft-target time budgets with larger hard safety caps (instead of strict hard abort at the configured pass timeout), and per-pass writer telemetry now includes prompt size + elapsed/timeout diagnostics (`promptChars`, `elapsedMs`, `softTimeoutMs`, `hardTimeoutMs`, `softTimeoutExceeded`) so timeout root causes are visible in run traces.
  - Writer reliability hardening landed: writer calls now honor remaining run deadline, use lower bounded retry/timeout budgets, and skip/compact-retry intelligently under low remaining time so repeated OpenAI timeout failures fail fast instead of consuming multi-minute run budget.
  - A dedicated memory retrofit plan is now documented under `docs/implementation/memory_implementation.md`, defining the spec-aligned path from clipped session summaries to a real three-layer memory model: raw live context, structured session working memory with recent outputs/artifacts, and durable retrieval-backed memory for later reuse across writing and future skills.
  - Session-memory groundwork is now in place for that retrofit: completed runs are promoted into bounded `recentOutputs` records inside working memory, session context now includes output availability (`body_available` vs `metadata_only`) and output metadata for planner continuity, and follow-up turns/tests now carry prior output records instead of relying only on `lastArtifacts` and prose summaries.
  - Writer outputs now persist by default into session-scoped artifacts when a draft is worth keeping but the caller did not supply `outputPath`, giving session memory a stable file handle for future follow-ups instead of leaving successful drafts trapped only inside tool-call telemetry.
  - Alfred can now ground short follow-up turns against recent session outputs through a dedicated model pass before planning: when `recentOutputs` exist, the orchestrator can resolve a turn like `Paste it here` to the right prior article/task (`source=recent_output`) instead of depending only on deterministic continuation phrases.
  - Grounded session outputs are now planner-visible artifacts, not just objective-rewrite hints: when a follow-up resolves to a prior session output, Alfred's planner receives the exact resolved output record including `artifactPath`, availability, preview, and metadata, so it can choose `file_read` and operate on the stored body directly.
  - Writing eval harness now has real-run replay coverage: added fixture-backed metrics for artifact completion, draft/citation evidence, writer failure rate, and writer call density using historical writing runs, so future regressions can be detected quickly from comparable runtime traces.
  - Source-quality gating for writing loops is now stronger: specialist evidence thresholds are dynamically scaled by requested draft length and citation requirements, planner context now exposes those thresholds, evidence-gap and low-budget finalize guards use them directly, and fetch progress now prefers `usablePageCount` over raw fetched-page totals to reduce low-quality-source drafting.
  - Specialist anti-thrash and graceful-exit behavior improved for writing runs: when remaining budget gets low and evidence is already reusable, research-agent now pivots search-heavy plans into draft-finalization actions, and guardrail exits now explicitly mark/report partial-result returns when best-effort draft/artifact evidence exists.
  - Alfred writing completion gate is now evidence-aware: turn-state now carries observed draft-word count, citation signals, and resolved output paths from runtime tool evidence, completion checks enforce word/citation requirements against those facts, and turn-state now loads existing run evidence before first planner step so valid preexisting writing artifacts can satisfy completion criteria without unnecessary extra loops.
  - Writer execution contract is now more resilient: runtime input repair normalizes article-format aliases (`article`, `news_article`, etc.) to canonical writer schema values, fallback writer drafts can now persist to requested output paths when they contain usable non-empty content, and orchestrator summaries now treat `writer_agent` and `article_writer` outputs consistently.
  - Specialist delegation/runtime now share one `AgentTaskContract` source of truth: Alfred builds and forwards task requirements (deliverable, citation/draft expectations, requested output path, target word count) and specialist loops consume the same contract instead of deriving divergent local defaults; citation-intent detection now recognizes `cite/cited/citations`, and requested output-path parsing trims trailing punctuation before handoff.
  - Specialist writing retries are now revise-first: when `writer_agent` fails but existing evidence is already usable, the loop retries with a focused revision pass before re-triggering search/fetch expansion, reducing retrieval thrash while preserving retrieval fallback when evidence is still thin.
  - Clarification behavior for loose research+writing asks is now planner-driven rather than deterministic pre-gating: Alfred can still ask one focused question when genuinely ambiguous, but does not short-circuit execution via hardcoded clarification prompts before planner reasoning.
  - Orchestrator control-mode routing is now command-only deterministic: normal user prompts no longer trigger regex-based diagnostic/plan-only gating, and explicit slash commands now control deterministic mode switches (`/diagnose|/debug|/run-diagnostics` for diagnostic mode, `/plan|/dry-run` for plan-only). Supporting prompt-parsing helpers in the orchestrator were rewritten without regex classifiers to keep non-command behavior model-driven.
  - Diagnostic-mode routing no longer hijacks normal writing prompts that include explanatory wording (for example “mention why”): execute-intent markers now take precedence over generic diagnostic markers, so article-generation requests are sent to normal execution instead of immediate run-diagnosis mode.
  - Follow-up continuity for autonomy replies is now preserved: short user messages like `You decide` are treated as continuation instructions (similar to `Proceed`/`Try again`) so Alfred keeps the prior substantive objective instead of resetting turn objective to the short reply.
  - Workspace chat execution stream is now cleaner and more useful: distilled thought/action lines include tool start/completion/failure and writer stage updates, raw internal `phase:eventType` labels are suppressed from chat bubbles, and the visible live trace window is larger for easier runtime diagnosis.
  - Writer tool transparency is improved: `writer_agent` now emits `writer_stage` events for structured attempt, compact retry, and persistence phases (start/completed/failed/skipped), making long writer latency/fallback behavior observable in timeline traces instead of a single black-box tool result.
  - Added `docs/architecture/writing-flow.md` to lock the agreed research-writing design: JSON control-plane orchestration, plaintext draft/revise generation, revision-first gap handling before expensive re-fetch loops, and one-round clarification behavior for ambiguous loose prompts.
  - Added `docs/architecture/turn-lifecycle.md`, a codex-style numbered walkthrough of Alfred’s live runtime flow (request entry, thread/turn state machine, orchestrator planner loop, specialist delegation, tool envelope traces, and persistence/event lifecycle) to support architecture reviews and debugging.
  - Specialist persist behavior now self-heals planner omissions: when objective text requests a save path and draft tools are called without `outputPath`, runtime injects the path (`persist_output_path_injected`) so successful drafts can write artifacts without extra deterministic fallback branching.
  - Specialist writer retries are now budget-aware: repeated writer fallbacks without any new evidence trigger `writer_retry_budget_guard` reroutes to retrieval tools, and persistent retry thrash emits `writer_retry_budget_exhausted` to stop wasting run budget.
  - Run-state JSON read resilience improved under concurrent tool execution: transient syntax-error reads now retry once before failing, reducing sporadic `Run not found` races seen during parallel specialist tool calls.
  - Specialist drafting now has an evidence-readiness guard: if planner asks for `writer_agent`/`article_writer` before enough retrieval evidence exists, runtime emits `specialist_plan_adjusted` (`insufficient_evidence_for_writer`) and reroutes to retrieval-first actions instead of burning writer attempts on low-evidence drafts.
  - Tool execution observability is now explicit: envelope execution emits `tool_action_started`, `tool_action_rejected`, `tool_action_completed`, and `tool_action_failed` events with redacted payload snippets so run timelines no longer treat tool calls as black boxes.
  - Naming clarity pass started: `article_writer` alias is now exposed alongside `writer_agent` in runtime tool discovery, policies, and planner contracts so specialist flows can migrate away from agent-like naming without breaking compatibility.
  - Writer/research reliability pass: `writer_agent` now retries once with a compact prompt after structured-call failures, emits `draftQuality` metadata, and avoids writing placeholder instruction-dump drafts to target artifact paths when transient model/network failures occur.
  - Specialist synthesis progress now ignores placeholder writer fallbacks, reducing false "draft complete" signals that previously sent research loops back into wasteful rediscovery passes.
  - Search manager now performs a lightweight primary probe when SearXNG healthcheck flaps, allowing Alfred to continue using primary search when it is still serving real results instead of prematurely defaulting to fallback providers.
  - Delegation-summary bug fix: Alfred now reports `research_agent` outcomes with drafting/fetch/citation/file metrics instead of lead/email metrics, eliminating misleading “no leads/email coverage low” summaries on blog/research tasks.
  - Workspace live trace verbosity increased: chat now renders the last 12 timestamped thought/action/observe/final trace lines (including tool/action payload snippets) to make runtime debugging possible without opening full JSON exports.
  - Workspace chat trace mode now shows a truthful live execution stream (raw planner thought/action/observation snippets with truncation) instead of paraphrased “friendly” status text, making it easier to verify what Alfred is actually doing mid-run.
  - Workspace chat live stream now includes distilled action updates (tool passes/delegation outcomes/guardrail stops) in addition to thought lines, while raw event labels remain isolated to Telemetry.
  - Orchestrator contract pass completed: Alfred now enforces per-turn execution permission (`execute` vs `plan_only`), routes run-id failure-analysis prompts to diagnostic mode more reliably, carries delegated tool evidence into completion evaluation, and specialist loops now return structured last-result summaries with a repeated-no-change success guard to avoid long status-only thrash.
  - Added standalone `web_fetch` and `email_enrich` tools so retrieval and contact enrichment can be executed independently of the monolithic `lead_pipeline` path.
  - Runtime separation is now active: `runReActLoop` enters an Alfred master-orchestrator loop that can delegate to `LeadGenAgent`, evaluate specialist outcomes against the current turn objective, and re-delegate under the same run budget/safety guardrails.
  - Explicit requested lead counts are now preserved end-to-end for small asks (`find 3 leads` stays `3`), preventing lead-agent deficit logic from drifting to inflated planner targets.
  - Workspace chat now renders as a continuous user/Alfred turn stream with higher body caps and long-text wrapping, reducing the isolated-card feel during multi-turn sessions.
  - Search fallback options now include Bright Data: when `BRIGHTDATA_SEARCH_API_KEY` is configured, gateway uses `brightdata` as fallback (ahead of `brave`) with configurable endpoint/path/engine/timeouts for quick provider testing.
  - Bright Data fallback call contract is now corrected for real runs: provider sends POST `/request` payloads with zone + target engine URL, and fallback activation now requires both API key and zone to avoid false-healthy but always-failing fallback states.
  - Added a `run_diagnostics` tool so Alfred can inspect run telemetry/debug exports directly and return structured failure summaries (tool errors, stop reasons, failure-signal aggregates, and remediation recommendations) without manual log parsing.
  - Added a `doc_qa` tool so Alfred can answer repository/documentation questions from local files with scoped retrieval, citation-rich responses, and deterministic fallback behavior when model support is unavailable.
  - Added a `writer_agent` tool for fast content drafting (memo/blog/email/outline/social/notes) with optional context-file grounding and optional write-to-file output, while preserving deterministic fallback behavior when model calls are unavailable.
  - Added runtime tool-allowlist injection for lead-agent execution, with explicit allowlist policy wiring at the ReAct handoff boundary.
  - Lead-agent runtime state now supports fetched-page scratch context (`setFetchedPages` / `getFetchedPages`) to prepare for deeper specialist tool-chaining.
  - Architecture boundary is now explicitly documented: `Alfred` remains the domain-agnostic orchestrator while `LeadGenAgent` is a specialist workflow with its own objective-clarification contract before retrieval.
  - Lead query planning now emits a structured `objectiveBrief` (summary + known constraints + missing constraints) and feeds that brief into extraction context, improving specialist self-alignment without hardcoding vertical assumptions.
  - Remaining SI/MSP-specific lead assumptions were removed from runtime fallback/planner phrasing and lead-tool descriptions, making the lead pipeline user-objective-driven across verticals/B2B/B2C/supplier use cases.
  - Runtime prompt-stack composition is now in place for Alfred (master persona + lead domain + role prompts), with prompt-version metadata emitted into timeline events so long sessions keep identity/behavior context refreshed on each planner/extractor call.
  - Auto-discovered local ops tools are now available to the agent (`file_list`, `file_read`, `file_write`, `file_edit`, `shell_exec`, `process_list`, `process_stop`) with project-root path safety and policy-mode gating (`trusted` required for shell/process stop).
  - Lead quality iteration completed: multi-query search fan-out and list-page company extraction are now implemented with regression coverage.
  - Sub-ReAct lead pipeline migration completed: strict lead schema, `sub_react_step` timeline events, batched structured extraction, and persistent Playwright browser pool.
  - Playwright setup is now mandatory for the lead pipeline path with one-time browser bootstrap support.
  - Run timeline UX now renders `sub_react_step` phases in a readable stepwise view while retaining raw JSON for debugging.
  - Lead target-count intent parsing is now robust for natural prompts (`find me 20...`) and can use model-planned count when available.
  - Extraction step now emits per-batch failure diagnostics for schema/API/content failures to make zero-result runs debuggable.
  - OpenAI HTTP failure diagnostics now include structured metadata (error type/code/message + request and rate-limit headers) and are surfaced in sub-ReAct timeline payloads for query-planning and extraction troubleshooting.
  - Strict JSON-schema compatibility fixes are in place for query/extraction structured outputs (nullable required fields), and search-stage events now include per-query failure detail so `urlCount: 0` runs are diagnosable from timeline data.
  - Live progress visibility is now improved with periodic backend heartbeat events (`observe:heartbeat` every 30s) and UI polling that streams run-state/timeline updates while queued/running.
  - Lead quality gate now applies employee-size-aware soft matching (in-range/near/unknown/out-of-range), includes conditional relax mode for high-deficit size-filtered runs, and exports size/selection metadata in CSV and run telemetry for clearer evaluation.
  - Size metadata extraction is now resilient: `sizeSource` was replaced with free-text `sizeEvidence` to avoid URL-format validation drops that previously zeroed entire extraction batches.
  - Extraction prompt quality is now upgraded with a schema-first system prompt that explicitly handles batched payloads, near-range confidence behavior, anti-hallucination constraints, and strict JSON output/repair expectations for higher extraction success.
  - Pass-2 agentic foundation started: tool registry is now folder-based auto-discovery with Zod input contracts, enabling model-directed tool invocation over `lead_pipeline`, `search`, and `write_csv`.
  - Lead-generation now runs through an outer agentic loop with iterative planning, action execution (single/parallel tools), observation history, replan triggers, and explicit stop-reason events (`target_met`, `budget_exhausted`, `diminishing_returns`, `tool_blocked`, `manual_guardrail`).
  - Agent-loop execution budgets are now configurable in `.env`, making iteration depth, parallelism, planner-call budget, observation window, and diminishing-return sensitivity tunable without code changes.
  - Planner structured-output reliability is fixed: tool inputs are now emitted as JSON strings (`inputJson`) to satisfy strict response-format schema validation, parsed before tool execution, and fallback sequencing now avoids repeated `write_csv` actions when planner calls fail.
  - Agentic recovery behavior is now enabled for search outages: Alfred can call `search_status` and `recover_search`, observe whether primary-provider restart succeeded, and replan from explicit search-failure telemetry (`searchFailureCount` and samples) instead of treating provider downtime as a static terminal condition.
  - Lead extraction/output now includes optional `email` as a first-class field across structured extraction, normalized candidate mapping, and CSV export.
  - User-initiated run cancellation is now supported end-to-end: backend cancel API, persisted cancel requests, cooperative stop checks through lead loops, partial-result persistence, and a UI `Cancel Run` action.
  - Lead-quality hotfixes now reduce false dedupe collapse (company-identity keying instead of `sourceUrl` domain fallback), and browse-stage diagnostics now include per-URL failure telemetry plus a Playwright navigation fallback path to improve scrape coverage on difficult pages.
  - Extraction prompt quality is now further tightened for employee-size normalization with explicit numeric parsing rules, conservative null behavior, and anti-aggregator naming constraints to improve size-aware lead scoring outcomes.
  - `.env.example` now includes a usable default `SEARXNG_START_CMD` so local SearXNG auto-recovery can be exercised without extra manual wiring.
  - Extraction contract now requires `employeeSizeText` with explicit `"unknown"` fallback and captures `emailEvidence`, improving structured completeness for size-aware gating and downstream outreach analysis.
  - Quality-gate scoring now treats `unknown` employee size as neutral and gives `near_range` a small positive boost, reducing unnecessary filtering pressure for close-fit leads while preserving stronger penalties for clearly out-of-range companies.
  - Agent loop now injects adaptive `lead_pipeline.minConfidence` defaults when absent (`0.70` first pass, `0.65` second pass, and `0.60` from third pass onward when deficit remains high) to improve recall without discarding early precision.
  - Planner-provided lead filters are now wired end-to-end (tool schema -> normalization -> sub-pipeline), so employee-size/country/industry/email-intent constraints are no longer silently discarded before query planning.
  - Lead pipeline now runs a dedicated `email_enrichment` browse pass on extracted company sites (`/`, `/contact`, `/contact-us`, `/about`) to fill missing `email`/`emailEvidence` and expose enrichment coverage/failure telemetry in run outputs.
  - Quality ranking now applies explicit email-aware scoring (email bonus + no-email penalty, stronger when email is requested), so outreach-ready leads are prioritized ahead of equally matched records missing contact data.
  - End-of-run assistant summaries are now deterministic and metric-based (no LLM narration), with explicit lead/email/failure/budget numbers to avoid optimistic wording that diverges from actual CSV/run telemetry.
  - Lead deduplication now keys by normalized company identity first (name + location, with common suffix normalization) before domain fallback, reducing duplicate rows caused by aggregator/profile URLs.
  - Planner/observe loop now has structured failure awareness (search/browse/extraction counts + LLM-budget exhaustion flag) and applies a deterministic guardrail to check `search_status` before re-running `lead_pipeline` after search-failure iterations.
  - Run budget handling is now deadline-aware end-to-end: the agent avoids launching deep lead passes when remaining time is too low, dynamically downscales crawl settings under tight budgets, and propagates deadline checks into browse/extract/enrichment so runs persist partial work and stop gracefully instead of overrunning.
  - Email enrichment is now explicitly budget-aware with dynamic URL caps and diminishing-return early-stop behavior, reducing wasted crawl time while preserving useful email-fill improvements under constrained run budgets.
  - Run-level LLM usage accounting is now wired end-to-end (OpenAI response usage capture -> sub-pipeline/tool propagation -> run-state aggregation/events/final summary), and planner context now includes aggregate failure signals so replans can explicitly react to repeated tool/search/extraction failures.
  - Web UI timeline now surfaces planner thoughts, failure snippets, and incremental/token-total usage summaries directly in the readable timeline view (while preserving raw JSON), improving live-debug visibility during long runs.
  - Timeline display is now thought-first and less noisy: planner action payload expansions and trailing raw JSON dumps were removed from the default view to keep run monitoring focused on meaningful progress/failure lines.
  - Agent loop now maintains dynamic budget modes (`normal`, `conserve`, `emergency`) with anti-thrashing hysteresis, emits explicit budget-mode-change events, and gives the planner budget snapshot context so low-budget replans can prefer cheaper/high-yield actions.
  - Planner context now includes a capped `pastActionsSummary` memory block (recent tool choices + outcomes + key inputs) to improve replan quality while keeping context bounded and avoiding runaway prompt growth.
  - Deterministic shell bounds are now budget-mode-aware: `lead_pipeline` action inputs are clamped differently for `normal`, `conserve`, and `emergency`, enabling constrained late-budget passes instead of prematurely terminating at a high fixed threshold.
  - Email-enrichment prioritization is now planner-driven: the lead tool accepts `runEmailEnrichment`, sub-pipeline honors planner skip directives, and planner context/prompt includes low-budget high-deficit hints so enrichment-vs-discovery tradeoffs remain agentic.
  - Final summaries now include a budget snapshot (mode + percent/absolute remaining time, tool, planner, and LLM headroom), and timeline stop events render this snapshot for easier budget-behavior analysis.
  - Diminishing-return stop behavior is now yield-aware (diagnostic actions no longer count as low-yield attempts), and planner context now carries a compact recent-performance summary (`yieldAttempts`, `yieldAdded`, `yieldZeroStreak`, failure counters) to reduce repeated ineffective retries.
  - Search reliability/observability is now stronger: primary SearXNG checks use retry + grace-window logic to reduce transient healthcheck dropouts, and search failures now carry structured stage/provider diagnostics that propagate into timeline/search-status outputs.
  - Planner tool-action safety now includes a `search.query` guardrail: invalid/missing query inputs are auto-corrected from the user request and logged as plan adjustments, preventing wasted iterations on schema-validation failures.
  - Lead-pipeline start guardrails are now budget-mode-aware (`normal=30s`, `conserve=15s`, `emergency=5s`) instead of a fixed cutoff, and planner instructions now explicitly require lightweight actions or stop decisions when under 60 seconds remaining.
  - Planner context now includes deficit strategy (`growth` vs `polish_only`), expected per-iteration LLM cap, and rolling yield-per-1k-token alerts; planning prompt guidance now uses these signals to avoid repeating low-efficiency actions and to prioritize polish behavior near target.
  - Running `lead_pipeline` now supports soft-stop behavior: once a tool is in flight and remaining budget drops under mode threshold, queued extraction batches and optional email enrichment are skipped gracefully, partial leads are preserved, and `stoppedEarlyReason` is emitted for debugging/replanning.
  - Planner low-yield behavior is now strategy-diverse by instruction (`query_diversify`, `relax_confidence`, `volume_no_email`, `deeper_crawl`, `stop`) and explicitly avoids repeating identical queries across consecutive low-yield iterations.
  - Default local test budgets were raised (`maxDuration=10m`, `plannerCalls=10`) and page navigation now tries `domcontentloaded` first with `networkidle` fallback for faster crawl throughput under typical pages.
  - Email enrichment quality is now stronger with a quick-scan pass (`/contact`, `/contact-us`, homepage) before full Playwright browsing, domain-aware business-email acceptance, duplicate-email scrubbing across leads, and timeline/output telemetry for quick-scan attempts/hits.
  - Agent loop now carries semantic failure signals (`semantic_miss`, `search_retrieval_blocked`, low-email coverage, budget exhaustion) into planner context as informative telemetry, improving replan quality without hard-coding one-to-one action rules.
  - Outer-loop adaptability is now less brittle: adaptive iteration ceiling scales with budget/yield, novelty guardrails prevent repeating near-identical low-yield lead-pipeline passes, and post-search-recovery behavior now requires at least one yield attempt before diminishing-returns stop is allowed.
  - GPT-5 model compatibility is now fixed in OpenAI client calls by omitting explicit temperature for GPT-5-family requests, preventing extraction/planner 400 failures caused by unsupported `temperature` overrides.
  - Repository hygiene update: root `debug/` exports are now ignored in git, preventing local debug timeline dumps from polluting git status during iteration.
  - Root `debug/` ignore was reverted to allow committing and sharing exported run timeline/debug files directly from the repository root when needed.
  - Search recovery now emits actionable diagnostics (command/exit/signal/stderr + runtime spawn errors) and surfaces `lastPrimaryRecovery` through `search_status`, while planner observation logic now treats direct `search` tool failures as search-failure signals for better outage-aware planning and stop behavior.
  - Added a root `README.md` with a concise project overview, high-level plan, current status snapshot, and pnpm-first local run/test instructions for faster onboarding and testing.
  - Added visual README masthead assets (`assets/alfred_masthead.jpg`, `assets/alfred_at_work.mp4`) and wired the image into the top of the root README for clearer project branding.
  - Expanded README narrative to include the long-term “second brain” product vision and explicitly frame lead generation as the focused first use case for measurable value validation.
  - Refined README positioning copy to emphasize the “There is no Batman without Alfred” framing and clearly sequence long-term second-brain vision before the lead-gen starting scope.
  - Deferred roadmap tracking moved to `docs/to_revisit.md` for browse-budget controls and LLM cap expansion after validation.
  - Test/build script wrappers were migrated from `npm` to `pnpm`, and `pnpm-lock.yaml` is now committed with `package-lock.json` removed.
  - Alfred orchestration now runs an explicit completion-evaluator pass after successful tool/delegation results, feeds evaluator notes back into planner context as reflection hints, and exits to a final answer when the latest result is already sufficient.
  - Lead-agent planning no longer mutates the model’s chosen action on search-failure or novelty patterns; those domain-specific overrides were replaced with reflection hints in planner context so the model remains in control while still seeing the failure pattern clearly.
  - Alfred now delegates specialist work through a generic skill registry and `runAgentLoop` dispatcher, with `lead_agent` registered as a first-class runtime skill instead of being hardcoded directly into the orchestrator.
  - Delegated skill runs now carry parent/delegation metadata plus a lightweight scratchpad scaffold, and Alfred emits explicit delegation start/result events so future sub-agents can share bounded run context without changing the current lead workflow semantics.
  - Delegation telemetry and scratchpad scaffolding now have direct orchestrator unit coverage, verifying that Alfred emits delegation start/result events and passes bounded turn context into delegated skill runs.
  - Final lead-run artifacts now persist directly into run state, and Alfred now propagates delegated `artifactPaths` immediately so session-level continuity can reference prior CSV outputs without waiting for a later run-store refresh.
  - Session continuity now uses bounded working memory: completed runs update `lastRunId`, `lastCompletedRunId`, artifact references, and compact summaries, which are injected into Alfred as `[Session Context]` on later turns instead of relying on deterministic follow-up parsing.
  - `/newsession` now clears only session working memory while preserving stored run history, and `session_context_loaded` telemetry plus integration/smoke tests cover both follow-up context injection and memory reset behavior.
  - Architecture direction is now explicitly documented around a live-context-first model: Alfred should use recent conversation directly, derive a canonical task brief only when execution begins, preserve explicit user requirements immutably through delegation/tooling, and defer memory compaction/QMD retrieval until context pressure requires it.
  - First runtime pass of the canonical-brief architecture is now active for lead generation: Alfred builds a `LeadExecutionBrief` at delegation time, passes it into `lead_agent`, and the lead runtime/pipeline now prefer that brief over downstream re-interpretation of the raw user message.
  - Follow-up runtime refinement is in place: lead-agent planning now uses the canonical brief for email intent and missing-search-query fallback, and Alfred's completion evaluator now sees that brief when judging delegated lead outcomes.
  - The lead sub-pipeline now uses the canonical brief for more fallback/extraction-prompt behavior, Alfred retains the current lead brief in planner-visible scratchpad state, and the first atomic specialist extraction tool (`lead_extract`) now exists alongside `lead_pipeline` so fetch/extract flows can evolve away from the monolith incrementally.
  - Lead retrieval now has its first shortlist-first atomic path: `lead_search_shortlist` stores fetch-worthy URLs, `web_fetch` can consume stored shortlist state, and fallback planning now prefers `web_fetch -> lead_extract -> email_enrich` before defaulting to the heavier `lead_pipeline` compatibility path.
  - Alfred orchestration now maintains a structured turn-state object with canonical brief, completion criteria, live retrieval facts, and last action summary; that state is injected into planner/evaluator prompts and emitted via `alfred_turn_state_updated` telemetry so replans are driven by explicit state instead of ad hoc summary strings alone.
  - Alfred turn-state now includes explicit completion-gap semantics: `completedCriteria`, `missingRequirements`, and `blockingIssues` are derived from both live state and persisted run/tool-call outputs, giving the planner/evaluator a concrete checklist of what is still missing from the current turn instead of only prose summaries.
  - Lead-agent planning now receives a state-derived `recommendedAtomicNextAction` hint and the fallback path mirrors that same shortlist -> fetch -> extract -> enrich progression, so the specialist is more likely to act through composable tools by default instead of jumping back to `lead_pipeline` too early.
  - Live-context-first session handling is now active: a bounded recent-turn buffer is stored in session working memory, the current user turn is injected before each run starts, and Alfred sees both compact session memory and recent conversation directly in prompt context so short-horizon follow-ups should stop feeling like brand-new isolated requests.
  - Hotfix applied for a zero-yield lead run regression: canonical lead briefs now anchor to the actual user turn (instead of planner-written delegation text), and `lead_search_shortlist` calls are sanitized pre-execution so oversized query/maxResults inputs no longer burn iterations on schema errors.
  - Additional runtime guardrails landed: Alfred now avoids first-turn clarification-only responses for executable lead requests by delegating immediately, and lead-agent now detects repeated no-progress `search_status` loops and pivots to recovery/shortlist actions to reduce diagnostic thrash.
  - Stability pass deepened with explicit execution-first telemetry (`alfred_plan_adjusted`) and repeated-diagnostic-loop rewrites in lead-agent planning, reducing planner/tool budget waste when health diagnostics repeat without changing recovery state.
  - Lead retrieval quality improved with objective-aware shortlist ranking: candidate URLs are now scored using company-type/geo/email signals, penalized for staffing/glossary/listicle patterns, boosted for contact/team/company relevance, and capped for domain diversity before fetch/extract.
  - Alfred now has a reusable specialist runtime for non-lead work: `runSpecialistToolLoop` powers new `research_agent` and `ops_agent` skills (with scoped tool allowlists), and orchestrator delegation guidance now routes by objective (lead generation vs research/drafting vs local operations) instead of treating lead-centric execution as the only specialist path.
  - Orchestrator control-plane now enforces turn-mode gating: diagnostic/meta prompts are answered with evidence-backed run summaries (`RunStore` records/events) and do not trigger specialist execution loops, and lead brief building now explicitly treats prior-session constraints as inactive unless the user reaffirms them in the current turn.
  - Workspace composer UX now matches chat expectations: `Enter` submits to Alfred, `Cmd/Ctrl+Enter` creates a newline, and the message box clears on submit (with restore only on send failure).
  - Control-plane hardening pass started: `TurnRuntime` now enforces a typed turn op/state machine and emits strict lifecycle events (`TurnStarted`, `TurnProgress`, `TurnComplete`, `TurnAborted`), and `ChatService` executes turns through this runtime so lifecycle semantics are consistent before planner/tool logic runs.
  - Added per-session `ThreadRuntime` + `ThreadRuntimeManager` with queued op sequencing and watcher events, and switched ChatService turn dispatch (including async queued turns) to submit through this runtime so each session has a single active turn with deterministic op ordering.
  - Specialist runtime now uses a shared normalized tool execution envelope (`input`, `result`, `error`, `durationMs`, `requiresApproval`) via a common router helper, making tool outcomes structurally consistent across specialist actions and improving downstream telemetry/debug reliability.
  - Reliability mechanics landed for runtime loops: OpenAI chat/structured calls now use retry+backoff on transient transport failures, planner failures are classified (`network|timeout|schema|policy_block`) with explicit fallback behavior in Alfred/specialist loops, and `RunStore` now writes events through a bounded `RunEventChannel` to prevent long-run event backpressure from overwhelming event persistence.
  - Persistence abstraction pass is in place: `RunStore` now depends on a pluggable `RunStorage` interface (default `JsonFileRunStorage` adapter), and lifecycle replay support (`replayLifecycle`) can reconstruct turn start/progress/terminal events for resume/replay diagnostics without changing existing JSON/JSONL persistence behavior.
  - Eval harness foundation is now added under `tests/evals/turn_runtime`: real debug run fixtures are replayed through metric computation (`wrongModeExecutionRate`, `evidenceFaithfulnessRate`, `uselessToolCallCount`, `yieldPerThousandTokens`) so runtime regressions can be caught against historical runs before shipping loop changes.
  - Workspace UI cleanup pass is applied for pre-test stability: chat flow now feels more conversational (lighter turn framing + timeline rail), active session visibility is clearer (`Live` session pill + live-run summary card), chat truncation limits are raised and preserve line breaks, and inspector scroll spacing is adjusted so lower cards remain reachable.
  - Composer input behavior now supports `Shift+Enter` for newline while preserving `Enter` as submit, reducing accidental partial prompt submissions during multi-line drafting.
  - Alfred turn execution now creates an immutable per-turn objective contract (`requiredDeliverable`, `hardConstraints`, `doneCriteria`) at loop start, stores it in scratchpad/telemetry (`alfred_objective_contract_created`), and injects it into planner + turn-state context so replans stay anchored to the same task contract throughout the run.
  - Completion Evaluator v2 is now contract-gated: even when the model requests `shouldRespond=true`, Alfred performs deterministic objective-contract checks (lead-count/email hard constraints, citation/draft requirements, missing requirement gaps) and can block premature responses with `alfred_completion_contract_blocked` telemetry.
  - `research_agent` now runs under an explicit specialist task contract (draft + citation expectations for research/blog asks), tracks progress signals (sources, fetched pages, draft words, citation count), blocks premature `respond` actions with `specialist_contract_blocked`, and emits evidence-first failure summaries when the contract cannot be satisfied within budget.
  - Tool execution now includes input auto-repair for malformed planner JSON payloads (markdown-fenced JSON, unicode quote normalization, object-block extraction, single-quote conversion, trailing-comma stripping), and specialist telemetry now records when repaired inputs were applied so invalid-input churn is easier to debug.
  - Specialist runtime now has a loop-shape guard for search-thrash: repeated search-family iterations without downstream progress (`web_fetch`/`lead_extract`/`writer_agent`/persist tools) trigger `specialist_loop_guard_triggered` and stop the loop early to conserve budget for replanning or user-directed retries.
  - Alfred orchestration now follows capability-driven runtime planning: every turn exposes the full discovered tool catalog plus all registered specialist agents, planner-driven `respond` choices are no longer force-rewritten into lead delegation, and delegation telemetry now carries explicit `skill`/result details to improve runtime transparency for blog/lead decision paths.
  - Unit-test stability improved for lifecycle replay coverage: `RunStore` replay tests now derive synthetic event timestamps from the run's own creation day, preventing date-mismatch flakes while preserving lifecycle reconstruction assertions.
  - Follow-up execute turns now preserve conversational objective context: shorthand messages like `Try again` are resolved against recent substantive user goals, Alfred emits `alfred_turn_objective_resolved` source telemetry, and objective contracts/planner input stay anchored to the original task rather than generic follow-up text.
  - Specialist loops now carry a phase-progress contract (discovery/fetch/synthesis/persist) and explicit `specialist_phase_transition_required` hints, reducing search-only thrash by pushing planner context toward downstream transitions when URLs are already discovered but content fetch/draft progress is missing.
  - Search-flaky handling is now adaptive inside specialist runtime: repeated timeout signals trigger a retry profile that narrows parallel search fan-out (`specialist_plan_adjusted`), encouraging smaller high-yield retries before guardrail stop.
  - Tool execution input repair now handles common planner shape mismatches before schema validation (query aliases like `queries[]`/`numResults`, writer plain-string instructions), reducing wasted iterations on avoidable `invalid_input_json` / `tool_schema_validation_failed` errors.
  - Specialist runtime now applies error-class-aware schema recovery (`specialist_plan_adjusted` reason `schema_recovery_forced`) after repeated input-schema failures, and adds phase-lock transitions that force discovery-to-fetch progression once URL discovery is already sufficient instead of repeating search-only loops.
  - Discovery-to-fetch handoff is now explicit and stateful for specialist loops: discovered URLs are normalized/reused directly when forcing `web_fetch`, avoiding empty `useStoredUrls` fetch calls after successful search discovery.
  - Specialist loops now maintain an explicit generic phase state machine (`discovery`, `fetch`, `synthesis`, `persist`, `complete`) with planner-visible phase context and `specialist_phase_state` telemetry, so progress tracking is reusable across non-lead workflows.
  - Specialist runtime now has diagnostic anti-thrash handling: repeated healthy `search_status` checks are auto-adjusted into productive actions (`search`/`shortlist`/`web_fetch`) via `specialist_plan_adjusted` (`diagnostic_thrash_guard`) to reduce planning-only loops.
  - Alfred completion gating is now evidence-bound for save-path tasks: turns that explicitly request writing output to a path now require matching artifact evidence before completion can pass, while clarification/progress-only replies remain allowed.
  - Durable session-output recovery is now wired into Alfred runtime: the orchestrator can recover recent outputs from persisted run history when working-memory `recentOutputs` are absent, merge them into session context, and load bounded artifact body previews so follow-up planning can reuse prior work generically without phrase-specific handlers.
  - Core-behavior refactor phase 1 is now in runtime: execute-mode turns get a model-owned interpretation contract before planning, objective contracts can carry semantic hints (assumptions, draft/citation intent, target word count, requested output path), and planner responses must explicitly label `final` vs `clarification` vs `progress` instead of Alfred inferring clarification from punctuation.
  - Core-behavior refactor phase 3 scaffolding is now in the shared agent runtime: Alfred, specialist, and lead tool contexts all carry generic active-work state (`assumptions`, `unresolvedItems`, `activeWorkItems`, `candidateSets`, `evidenceRecords`, `synthesisState`) so later planner/model passes can reason over persisted work structure without introducing domain-specific gates.
  - Specialist planner context now receives a generic `activeWorkState` snapshot derived from runtime signals rather than task-specific gates: candidate pools can come from discovered URLs/shortlists/leads, evidence records come from source cards/fetched pages/artifacts, and synthesis readiness is exposed as a reusable runtime summary for the model to reason over.
  - Specialist draft execution now uses a shared `writerReadiness` contract derived from evidence coverage plus synthesis state, so evidence-gap rerouting, revise-vs-retrieve retries, and low-budget finalize decisions all consume one generic readiness assessment instead of duplicating scattered writer-threshold heuristics.
  - Specialist completion gating is now more evidence-driven: long-form `respond` attempts can be blocked even when they clear superficial length thresholds if the active work state shows no supporting evidence or unresolved synthesis gaps, and stop/failure telemetry now includes unresolved-work plus synthesis-state detail for debugging.
  - Research-specialist convergence is tighter for non-draft tasks: research contracts now carry `requiresAssembly`, specialist planner responses must label `responseKind`, default follow-up clarification is locked once Alfred has already interpreted the turn as executable, synthesis readiness is split from final completion gaps (citations/artifacts), and synthesis-phase search loops can now reroute into an assembly pass before low-budget writer fallback.
  - Phase 4/5 runtime tightening continued: specialist assembly/fallback decisions now require a viable writer time window instead of forcing fetch-to-synthesis jumps, and `writer_agent` no longer persists low-confidence placeholder drafts from failed low-budget/no-output passes as fresh artifacts while still honoring explicit-path overwrite and complete-draft preservation rules.
  - Specialist convergence now has stronger fetch handoff mechanics: once runtime state is clearly `fetch_pending`, search-only replans are rewritten into downstream fetch/evidence actions, and planner timeouts in that state can recover into a mechanical fetch step instead of wasting another iteration on shortlist churn.
  - Fresh-turn interpretation is now less sticky to old artifacts: when turn grounding resolves to a standalone current-message objective, Alfred strips stale session-output paths and artifact obligations from the new task contract unless the user explicitly included that path in the current turn.
  - Phase 4/5 finalization is now in place: specialist stop/failure paths emit explicit output availability state (`body_available`, `metadata_only`, `missing`), contract gating uses that state during finalization, and placeholder/low-evidence outcomes no longer blur into reusable completed drafts.
  - Architecture docs are now synced to the current runtime: `docs/architecture/corebehaviour_refactor.md` reflects implemented vs pending refactor phases, `docs/architecture/turn-lifecycle.md` now documents turn grounding, recent-output recovery, active-work state, writer readiness, and output availability, and `docs/architecture/writing-flow.md` now describes the current memory-aware research-to-writing path instead of the older simplified pipeline.
  - Writer core has been refactored away from the old section-first pipeline: `writer_agent` now uses a provider-agnostic shape-aware path (`intent -> direct draft -> semantic review -> repair`) so ranked lists, comparisons, and other non-article deliverables are drafted in their final requested shape instead of collapsing into section plans or evidence memos.
  - Runtime output-availability semantics are now writer-quality-aware: specialist loops record last writer deliverable status/availability, assembly tasks only count as complete once a reusable synthesized body exists, and recovered placeholder writer artifacts stay `metadata_only` instead of being mistaken for finished bodies in later turns.
  - Fresh-turn grounding and fetch convergence are now tighter: Alfred demotes stale `recent_output` grounding when a substantive new request did not actually ask for prior artifact reuse, and research specialists now rewrite repeated `file_read` / `file_read + search` plans into `web_fetch` once runtime state is already `fetch_pending`.
  - Specialist source discovery is now hardened against memo URL poisoning: `file_read` payload text no longer auto-populates discovery `sourceUrls`, and URL extraction now strips trailing citation artifacts (for example `/n[S2]`) so malformed citation strings cannot drive repeated degraded `web_fetch` loops.
  - Specialist planner actions now use strict single-action adapters: when planner returns known tools with missing `singleInputJson`, runtime injects safe default inputs (`single_action_input_defaulted`) so loops execute instead of idling on `planner_returned_no_action`.
  - Specialist runtime now tracks per-iteration action economy (value delta, cost score, efficiency) in planner/observe context and triggers a global `low_value_action_thrash` loop guard after repeated low-yield cycles, reducing token burn on stagnant paths.
  - Planner context now includes a first-class per-tool input contract pack (`required`, `bounds`, `exampleInput`) for both Alfred and specialist loops, so model-generated tool JSON can follow hard limits more reliably (for example `maxResults <= 15`) instead of inferring constraints only from hints.
  - `web_fetch` now applies a quality gate before persisting pages into agent state: blocked/captcha/404/very-low-text pages are downgraded, usable pages are prioritized for downstream extraction/writing, and degraded primary batches trigger retry fetches from alternate candidate URLs with explicit quality/retry telemetry.
  - Research runs now maintain a lightweight source-card scratchpad inside agent state (captured from search/shortlist/fetch observations) and feed that structured evidence into `writer_agent`, so drafting is grounded in explicit interim claims/URLs instead of relying only on raw fetched page blobs.
  - Research-specialist reliability pass landed: `research_agent` now only sees research/drafting tools (no lead-specific extraction/enrichment/csv tools), specialist single-action fallback now repairs malformed single-tool JSON plans before execution, and tool input repair now sanitizes malformed `web_fetch` URL payloads plus common alias fields (`queries`, `pageLimit`, etc.).
  - Workspace thinking feed cleanup landed: chat/right-rail now render distilled thought summaries (including after run completion) and stop surfacing raw internal event labels like `thought:specialist_plan_created` in conversational UI surfaces.
  - Workspace chat now surfaces a lightweight, throttled “Alfred thinking...” stream from distilled thought/heartbeat events (last few lines only), while internal event labels remain in Telemetry to keep chat conversational instead of implementation-jargony.
  - Web UI pass completed: the browser client now uses a real Alfred cockpit shell with left-side navigation/sessions, a center workspace + telemetry/status/settings surface, and a right-rail for live heartbeat/thought/tool/budget context while keeping chat and raw telemetry rendering bounded for browser stability.
  - Telemetry is now a proper session -> run -> trace workflow with formatted event logs, raw JSON view, and direct run export from the browser, making it much easier to capture exact run bundles for debugging.
  - Frontend automated coverage is still limited to build/syntax validation (`pnpm run build`, `node --check webui/app.js`); a browser-level UI regression harness remains a follow-up item once the shell stabilizes.
  - Browser performance pass completed: the UI now avoids repainting hidden pages during polling, skips redundant `innerHTML`/`textContent` updates, polls less aggressively, and uses lighter panel styling to reduce lag on long-running sessions.
  - Right-rail clipping is fixed: the sticky inspector column now scrolls vertically instead of hiding overflow, so lower cards such as tokens/time and run controls remain reachable on smaller viewports.
  - March 17 deep-cut refactor landed: `research_agent` was removed from the public skill registry/tool-policy surface, Alfred no longer runs a separate completion-evaluator model after successful actions, and `writer_agent` is now a plain one-pass generation tool with mechanical quality/persistence checks instead of the old intent/draft/review/repair stack.
  - Phase 6 cleanup is now in progress in code: Alfred's fallback objective contracts no longer reconstruct draft/citation/word-count semantics from raw prompt text when turn interpretation is unavailable, main-loop lead-brief creation now depends on model-owned `taskType=lead_generation`, and regression coverage proves missing interpretation falls back to a minimal generic contract plus explicit output-path mechanics only.
  - Specialist runtime is now less semantic and more contract-driven: writer schema-repair/default inputs rebuild from the canonical contract, assembly/finalize behavior no longer keys off `research_agent` skill-name checks, and fallback specialist setup now degrades to a minimal generic contract unless an explicit task contract is provided.
  - March 17 refactor target is now implemented for the current runtime boundary: Alfred owns general-turn semantics through one turn contract and one local execution loop, writer is a plain generation tool, outer completion-evaluator indirection is gone, specialist helpers are contract/mechanics-driven, and Alfred-local general research now has mechanical search-to-fetch convergence instead of repeated broad-search replans.
  - Turn-contract hardening now blocks over-literal semantic drift from interpretation output: `markdown` no longer becomes a fake output-path obligation, and per-item source URL requirements no longer auto-upgrade into formal citation gates.
  - Workspace chat text selection is now stable during copy/paste: selecting text inside chat bubbles no longer triggers chat-card reloads, and message/thinking text is explicitly selectable in the browser UI.
  - Additional resiliency and production hardening tasks remain for later iterations.

## Remaining Follow-ups

- Add robust lead parsing quality heuristics and optional provider-based verification.
- Enable robots-aware scraping mode for production profile.
- Add structured retry/backoff metrics and deeper async supervision dashboards.
- Add lightweight browser-level regression coverage for the `webui/` shell once the new layout and telemetry interactions settle.
  - Self-contained LLM provider layer is now packaged for the refactored runtime: `openAiClient` exposes timeout/attempt-aware chat diagnostics, `src/services/llm/` provides provider interfaces plus fallback routing/OpenAI adapter, and dedicated `llmRouter` tests now cover provider failover/ordering so the simplified writer builds and runs cleanly on a fresh checkout.
